package com.grsu.tick.tack.toe.desktop;

import javax.swing.*;
import java.awt.*;

public class MainFrame {

    public MainFrame() {
        /*this.setTitle("TickTackToe");
        this.setLayout(new BorderLayout());
        //this.add(mainPanel, BorderLayout.CENTER);
        this.setSize(new Dimension(800, 650));
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.setLocationRelativeTo(null);
        this.setResizable(false);
        this.setVisible(true);

        JPanel menuPanel = new JPanel();

        JButton newGameButton = new JButton();
        newGameButton.setName("newGame");
        newGameButton.setText("New Game");
        newGameButton.setSize(100, 50);
        newGameButton.setActionCommand("123123124");
        newGameButton.addActionListener((actionEvent) -> {
            new GameFrame().setVisible(true);
            this.dispose();
        });

        JButton newGameWithBot = new JButton();
        newGameWithBot.setName("newGame");
        newGameWithBot.setText("New Game");
        newGameWithBot.setSize(100, 50);

        menuPanel.add(newGameButton);
        menuPanel.add(newGameWithBot);

        this.add(menuPanel);*/
    }
}
